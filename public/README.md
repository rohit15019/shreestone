# Public Assets Folder

Place static image files (such as `logo.png`, `logo.svg`, or `favicon.ico`) in this folder.
Files placed here can be accessed in your code via `/filename.png` (e.g., `/logo.png`).
